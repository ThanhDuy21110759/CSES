//Apple Division
#include <bits/stdc++.h>
#include <vector>
#define inp(arr); for (int i = 0; i < n; i++) cin >> arr[i];
using namespace std;
typedef long long ll;
int main(){
	int n; cin >> n;
	vector<ll> arr(n);
	inp(arr);
	return 0;
}

